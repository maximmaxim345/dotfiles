---
name: commit
description: Create a commit following `~/.claude/commit-guidelines.md`
---

Read `~/.claude/commit-guidelines.md` and create a commit following it.

Compose the commit message inline. Do not delegate to a subagent. For the subagent variant use `/commit-subagent`.
