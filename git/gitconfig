# Generated by dotfiles installer - modify as needed
# Marker: DOTFILES_MANAGED_GITCONFIG_MAXIMMAXIM345

[color]
	diff = auto
	status = auto
	branch = auto
	ui = true
[mergetool]
	keepBackup = false
[init]
	defaultBranch = main
[rerere]
	enabled = false
[core]
	untrackedCache = true
	editor = vim
[branch]
	sort = -committerdate
[rebase]
	updateRefs = true
[user]
	email = EMAIL
	name = NAME
	signingkey = /home/.../.ssh/id_ed25519.pub
[gpg]
	format = ssh
; [commit]
; 	gpgsign = true
[credential "https://github.com"]
	helper = 
	helper = !gh auth git-credential
[credential "https://gist.github.com"]
	helper = 
	helper = !gh auth git-credential

## true on windows, input on unix
#[core]
#	autocrlf = input
# Use the following section to include a .gitconfig file in a specific directory
#[includeIf "gitdir:~/..."]
# path = ~/.../.gitconfig
