# Music Assistant frontend

- Base branch is `main`. Use `pnpm`.
- Reuse design-system components and their variants (`Button`, `secondary` badge) instead of custom styles or colors, and check dark mode.
- PRs: fill the template as-is. Keep the "Related issue" header and leave it empty when there's no issue, never the placeholder. Tick exactly one "Types of changes" box and the AI policy box. The title becomes the changelog line.
